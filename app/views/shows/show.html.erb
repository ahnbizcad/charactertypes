<p id="notice"><%= notice %></p>

<h1><%= @show.title %></h1>
<%= link_to 'Edit', edit_show_path(@show) %>
<%= @show.show_type %>
<%= @show.year %>

<br />
<h2>Characters</h2>
<% @show.characters.each do |c| %>
	<%= link_to c.name, c %><br />
<% end %>
<br />

<%= link_to 'Add a Character', new_character_path(:show => @show) %>
<br />
<br />
<%= link_to 'List of Shows', shows_path %>